{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "tmetric",
  "title": "TMetric Timer",
  "description": "Start and stop TMetric timers from Raycast",
  "icon": "command-icon.png",
  "author": "you",
  "categories": ["Productivity"],
  "license": "MIT",
  "commands": [
    {
      "name": "start-timer",
      "title": "Start Timer",
      "description": "Start a new TMetric timer",
      "mode": "view"
    },
    {
      "name": "stop-timer",
      "title": "Stop Timer",
      "description": "Stop the currently running TMetric timer",
      "mode": "no-view"
    },
    {
      "name": "timer-status",
      "title": "Timer Status",
      "description": "View the current TMetric timer status",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "apiToken",
      "title": "API Token",
      "description": "Your TMetric API token",
      "type": "password",
      "required": true
    },
    {
      "name": "useMockApi",
      "title": "Use Mock API",
      "description": "Use a fake in-memory API instead of real TMetric (for testing)",
      "type": "checkbox",
      "default": false,
      "label": "Enable Mock API",
      "required": false
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.93.2",
    "@raycast/utils": "^1.17.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "~22.13.4",
    "@types/react": "^19.0.8",
    "eslint": "^8.57.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish",
    "test": "vitest run"
  }
}
